#include <bits/stdc++.h>
#define fi frist
#define se second
using namespace std;
typedef long long ll;
const long long INF = 1e18 + 1;

void dfs(v) {
    //đánh dấu v là đã thăm
    //for tất cả đỉnh v mà tồn tại cạnh (u, v):
    //  nếu chưa thăm v:
    //      dfs(v):
}

int main() {
    ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
    //freopen(".inp", "r", stdin);
    //freopen(".out", "w", stdout);


    //dfs khử đệ quy
    /*
    stack st
    st <- u
    while(!st.empty())
        v <- st.top()
        st.pop()
        nếu v chưa thăm thì
            đánh dấu đã thăm v
            for i tất cả các cạnh của v
                nếu tồn tại đỉnh chưa thăm
                    cho đỉnh vào stack
    */


    return 0;
}
